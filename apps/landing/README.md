# Landing app

Website/landing UI (TanStack + Vite) for Smart Address.

## Development

From the repo root:

```bash
pnpm install
pnpm --filter landing dev
```

## Build / preview

```bash
pnpm --filter landing build
pnpm --filter landing preview
```

## Tests

```bash
pnpm --filter landing test
```
