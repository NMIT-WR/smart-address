# Data ownership

Every query is stored in SQLite with:

- normalized query fields
- strategy
- suggestion count and errors
- full JSON result

This lets us build a first-party address database and reduce provider costs over time.
